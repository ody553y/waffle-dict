# Step 5 — Phase 3 Tasks for Codex
**Date:** 29/03/2026

## Phase 2 Status — DONE

All 8 Phase 2 tasks are complete and verified:

**Track A — Settings & UX:**
- Settings toggles (`pasteIntoActiveApp`, `copyToClipboardAsFallback`, `showTranscriptInMenu`) wired to MenuBarView behaviour ✓
- Global hotkey ⌥Space via CGEventTap on background thread, graceful degradation without Accessibility ✓
- Floating dictation panel: non-focus-stealing NSPanel with waveform → spinner → success/error → auto-dismiss ✓

**Track B — Model Management:**
- Model catalog with bundled `models-manifest.json` (5 Whisper + 1 Parakeet) ✓
- Resumable model download with SHA-256 checksum verification ✓
- Models settings tab: browse, download with progress, remove ✓
- Model picker in General settings + MenuBarView, with fallback logic ✓
- Parakeet backend registered in worker (gated on NeMo availability) ✓

`swift build` passes. 17 Swift tests pass. 10 Python tests pass.

---

## Phase 3 Scope

Phase 3 turns Waffle from a dictation tool into a **transcription workspace**. Two parallel tracks:

**Track A — File Transcription Workspace** (new UI surface, GRDB persistence, export)
**Track B — Production Hardening** (real model URLs, streaming checksum, language picker, error polish)

Do Track A first — it's the major new feature. Track B can be interleaved or done after.

---

## Track A: File Transcription Workspace

### Task A1: GRDB Transcript History — Schema + Service
**Target:** New `Sources/WaffleCore/TranscriptStore.swift`, update `Package.swift`

Add GRDB as a dependency and create the transcript persistence layer.

1. Add `GRDB` Swift package: `https://github.com/groue/GRDB.swift`, from `7.0.0`
2. Define `TranscriptRecord`:
   ```
   id: Int64 (autoincrement primary key)
   createdAt: Date
   sourceType: String ("dictation" | "file_import")
   sourceFileName: String? (original filename for imports, nil for dictation)
   modelID: String
   languageHint: String?
   durationSeconds: Double?
   text: String
   ```
3. Create `TranscriptStore`:
   - `init(databasePath:)` — opens/creates the SQLite database at `~/Library/Application Support/Waffle/transcripts.sqlite`
   - `save(_ record: TranscriptRecord) throws -> TranscriptRecord` — inserts and returns with assigned ID
   - `fetchAll(limit:offset:) throws -> [TranscriptRecord]` — newest first
   - `search(query:limit:) throws -> [TranscriptRecord]` — full-text search on `text` column using FTS5
   - `delete(id:) throws`
   - `fetchOne(id:) throws -> TranscriptRecord?`
4. Set up FTS5 virtual table on `text` for fast search
5. Database file location: `~/Library/Application Support/Waffle/transcripts.sqlite`

**Tests:** Unit test save, fetch, search, delete against an in-memory database.

**Acceptance:** `TranscriptStore` round-trips records. FTS search returns matching results. Tests pass.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Implemented in `Sources/WaffleCore/TranscriptStore.swift` with GRDB schema + migrator, FTS5 table, and CRUD/search APIs.
- Added GRDB dependency in `Package.swift` (`https://github.com/groue/GRDB.swift`, from `7.0.0`).
- Added tests in `Tests/WaffleCoreTests/TranscriptStoreTests.swift` covering save/fetch/search/delete (`:memory:` DB).
- Verified with `swift test` (all tests passing).

---

### Task A2: Save Dictation Transcripts to History
**Target:** `Sources/WaffleApp/DictationController.swift`

Wire `TranscriptStore` into the dictation flow so every successful transcription is persisted.

1. Add `TranscriptStore` as a dependency of `DictationController`
2. After a successful transcription in `stopAndTranscribe()`, save a `TranscriptRecord` with:
   - `sourceType: "dictation"`
   - `sourceFileName: nil`
   - `modelID`: the model that was used
   - `text`: the transcription result
   - `durationSeconds`: compute from the recording duration if available, or nil
3. Do not block the paste/copy delivery on the save — fire-and-forget with error logging

**Tests:** None required — existing flow tests still pass, persistence is tested in A1.

**Acceptance:** After a dictation, a record appears in the SQLite database. Existing 17 Swift tests still pass.

**Dev Notes (30/03/2026):**
- Status: **Done**
- `DictationController` now receives `TranscriptStore` and saves successful dictations asynchronously (fire-and-forget).
- `AppDelegate` now creates a shared `TranscriptStore` and injects it into `DictationController`.
- Added duration helper in `AudioCaptureService.recordingDurationSeconds(for:)` and persisted that value when available.
- Verified with `swift test` (all tests passing).

---

### Task A3: Transcript History View
**Target:** New `Sources/WaffleApp/TranscriptHistoryView.swift`, update `Sources/WaffleApp/WaffleApp.swift`

Add a new window showing transcript history with search.

1. Create `TranscriptHistoryView`:
   - List of transcripts, newest first, with: date, source badge ("Dictation" / filename), model name, first 120 chars of text
   - Search bar at the top — queries `TranscriptStore.search(query:)`
   - Click a row to expand and show full text
   - Toolbar buttons: "Copy Text", "Export", "Delete"
   - Pagination: load 50 at a time, load more on scroll
2. Register the window in `WaffleApp.swift` as a new `Window` scene, opened from MenuBarView via a "History" button
3. Add a "History" button to MenuBarView between "Settings" and "Quit"

**Notes:**
- Use `@Observable` or `@StateObject` for the view model
- The window should be a standard resizable window, not a panel
- Keep the history view simple — no inline editing yet

**Tests:** None required for UI.

**Acceptance:** User can open the History window, see past transcriptions, search by text, copy, and delete entries.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Added `Sources/WaffleApp/TranscriptHistoryView.swift` with:
  - Search field
  - Newest-first transcript list
  - Row expand/collapse for full text
  - Toolbar actions for Copy / Export / Delete
  - Pagination (50 records with incremental loading)
- Added `Window("History", id: "transcript-history")` scene in `WaffleApp.swift`.
- Added `History` button to `MenuBarView` between `Settings…` and `Quit`.
- Included local-storage note and empty/search-empty states inspired by Spokenly.
- Verified with `swift test` (all tests passing).

---

### Task A4: File Import — Drag-and-Drop Transcription
**Target:** `Sources/WaffleApp/TranscriptHistoryView.swift`, new `Sources/WaffleCore/FileTranscriptionService.swift`

Add the ability to import audio files for transcription (not just live dictation).

1. Create `FileTranscriptionService` in WaffleCore:
   - `transcribe(fileURL: URL, modelID: String, languageHint: String?) async throws -> FileTranscriptionResult`
   - Uses `WorkerClient.transcribeFile()` under the hood
   - Returns `FileTranscriptionResult` with `text`, `durationSeconds`, `backendID`
2. Add a drag-and-drop zone to `TranscriptHistoryView`:
   - Accepts `.wav`, `.mp3`, `.m4a`, `.flac`, `.ogg` file types
   - On drop: shows an inline progress row → transcribes → saves to `TranscriptStore` with `sourceType: "file_import"` and `sourceFileName`
   - Error state shown inline on the row
3. Add an "Import File…" button in the toolbar that opens an `NSOpenPanel`
4. One file transcription at a time (queue additional drops)

**Notes:**
- The worker already accepts any file path — no worker changes needed
- For non-WAV files, the worker's `faster-whisper` handles format conversion internally
- Parakeet may need WAV input — document this limitation in the UI if applicable

**Tests:** Unit test `FileTranscriptionService` with a mocked `WorkerClient`.

**Acceptance:** User can drag a WAV file onto the History window, watch it transcribe, and see the result saved in history.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Added `Sources/WaffleCore/FileTranscriptionService.swift`:
  - `transcribe(fileURL:modelID:languageHint:)`
  - Uses `WorkerClient.transcribeFile()` through a protocol abstraction (`WorkerFileTranscribing`) for testability
  - Returns `FileTranscriptionResult(text, durationSeconds, backendID)`
- Added unit test `Tests/WaffleCoreTests/FileTranscriptionServiceTests.swift` with mocked worker client.
- `TranscriptHistoryView` now supports:
  - Drag-and-drop (`.wav`, `.mp3`, `.m4a`, `.flac`, `.ogg`)
  - `Import File…` button (`NSOpenPanel`)
  - Inline queued/transcribing/failed rows
  - Single-file processing queue (one active transcription at a time)
  - Saving imported transcripts to `TranscriptStore` as `sourceType: "file_import"` with `sourceFileName`
- Added Parakeet note in UI about WAV preference.
- Verified with `swift test` (all tests passing).

---

### Task A5: Export Transcripts — SRT, VTT, JSON, Markdown
**Target:** New `Sources/WaffleCore/TranscriptExporter.swift`

Add export functionality for transcripts.

1. Create `TranscriptExporter` with static methods:
   - `exportAsPlainText(_ record: TranscriptRecord) -> String`
   - `exportAsMarkdown(_ record: TranscriptRecord) -> String` — includes metadata header (date, model, source)
   - `exportAsJSON(_ records: [TranscriptRecord]) -> Data` — array of records with all metadata
   - `exportAsSRT(_ record: TranscriptRecord) -> String` — single block SRT (no timestamp segmentation yet, just `00:00:00,000 --> duration`)
   - `exportAsVTT(_ record: TranscriptRecord) -> String` — WebVTT equivalent of SRT
2. Wire export to the History view's "Export" toolbar button:
   - Shows `NSSavePanel` with format picker (Plain Text, Markdown, JSON, SRT, VTT)
   - Default filename: `{source_name}_{date}.{ext}`
3. Support exporting multiple selected records as JSON (batch export)

**Notes:**
- SRT/VTT timestamps are approximate for now (single segment spanning full duration). Proper segmentation requires word-level timestamps from the worker — that's a future enhancement.
- JSON export uses the same field names as `TranscriptRecord` for round-trip compatibility.

**Tests:** Unit test each export format against a fixture `TranscriptRecord`.

**Acceptance:** User can export a transcript as any of the 5 formats. Exported files are valid and well-formatted.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Added `Sources/WaffleCore/TranscriptExporter.swift` with:
  - `exportAsPlainText(_:)`
  - `exportAsMarkdown(_:)` (metadata header includes date/model/source, plus optional language/duration)
  - `exportAsJSON(_:)` (ISO-8601 dates, pretty/sorted output)
  - `exportAsSRT(_:)` (single-segment timestamp format)
  - `exportAsVTT(_:)` (single-segment WebVTT format)
- Added tests in `Tests/WaffleCoreTests/TranscriptExporterTests.swift` for all export formats.
- Wired History `Export` button to `NSSavePanel` with format picker (Plain Text / Markdown / JSON / SRT / VTT).
- Added multi-select export support:
  - Command-click to select multiple rows
  - Multi-selection export constrained to JSON batch export
  - Single-selection export supports all 5 formats
- Default filename format now follows `{source_name}_{date}.{ext}` for single exports.

---

## Track B: Production Hardening

### Task B1: Streaming SHA-256 Checksum for Large Models
**Target:** `Sources/WaffleCore/ModelDownloadService.swift`

The current `checksum(for:)` loads the entire file into memory. For `whisper-large-v3` (3.1GB) this will spike memory.

1. Replace `Data(contentsOf:)` with a streaming reader:
   - Open file handle, read in 1MB chunks, feed each chunk to `SHA256.update()`
   - Return final digest
2. Keep the existing `verifyChecksum(of:expectedChecksum:)` API unchanged

**Tests:** Existing checksum tests should still pass. Add a test with a file larger than 1MB to verify streaming works.

**Acceptance:** `verifyChecksum` works on large files without loading them entirely into memory. Existing tests pass.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Updated `ModelDownloadService.checksum(for:)` to stream file contents in 1MB chunks using `FileHandle.readData(ofLength:)` and incremental `SHA256.update(data:)`.
- `verifyChecksum(of:expectedChecksum:)` API remains unchanged.
- Added regression test `verifyChecksumHandlesFilesLargerThanOneMegabyte()` in `Tests/WaffleCoreTests/ModelDownloadServiceTests.swift`.
- Verified with `swift test --filter ModelDownloadServiceTests` and full `swift test` (all tests passing).

---

### Task B2: Language Picker in Settings and Dictation Flow
**Target:** `Sources/WaffleApp/SettingsView.swift`, `Sources/WaffleApp/DictationController.swift`

Currently `languageHint` is always `nil`. Add a language picker.

1. Add a `@AppStorage("languageHint")` setting in GeneralSettingsView:
   - Picker with options: "Auto-detect" (nil), plus a curated list of common languages: English, Spanish, French, German, Italian, Portuguese, Chinese, Japanese, Korean, Russian, Arabic, Hindi
   - Store the ISO 639-1 code (e.g. "en", "es") or empty string for auto-detect
2. `DictationController` reads `languageHint` and passes it to `WorkerClient.transcribeFile()`
3. The picker should indicate which languages are supported by the selected model:
   - Parakeet: English only — show a note and disable the picker when Parakeet is selected
   - Whisper: multilingual — all options available

**Tests:** None required for UI.

**Acceptance:** Setting a language hint to "en" sends `language_hint: "en"` to the worker. Auto-detect sends `null`.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Added `@AppStorage("languageHint")` in `GeneralSettingsView` with:
  - `Auto-detect` (`""`)
  - Common language options storing ISO 639-1 codes (`en`, `es`, `fr`, `de`, `it`, `pt`, `zh`, `ja`, `ko`, `ru`, `ar`, `hi`)
- Added model-aware language UX:
  - Picker is disabled when Parakeet is selected
  - Note clarifies Parakeet is English-only
- `DictationController` now computes and sends `languageHint` in `FileTranscriptionRequestPayload`:
  - Whisper: stored setting (`""` becomes `nil` for auto-detect)
  - Parakeet: forces `"en"` for compatibility
- Saved transcript history now stores the same effective `languageHint` used for dictation.
- Verified with full `swift test` (all tests passing).

---

### Task B3: Worker — Word-Level Timestamps in Response
**Target:** `worker/waffle_worker/server.py`, `worker/waffle_worker/models.py`, `worker/waffle_worker/backends/faster_whisper.py`

Add optional word-level timestamp data to the transcription response for future SRT/VTT segmentation.

1. Extend `FileTranscriptionResponse` with an optional `segments` field:
   ```python
   @dataclass(frozen=True)
   class TranscriptionSegment:
       start: float    # seconds
       end: float      # seconds
       text: str

   @dataclass(frozen=True)
   class FileTranscriptionResponse:
       job_id: str
       backend_id: str
       text: str
       segments: list[TranscriptionSegment] | None = None
   ```
2. Update `FasterWhisperBackend.transcribe_file()`:
   - Return segments from `faster-whisper` (it already provides them)
   - Map each segment to `TranscriptionSegment(start, end, text)`
3. Update `ParakeetBackend.transcribe_file()`:
   - Return `segments=None` (NeMo doesn't provide word-level timestamps in the basic API)
4. Update the server to include `segments` in the JSON response when present
5. Update Swift `FileTranscriptionResponse` to decode `segments` as optional

**Tests:** Update the existing transcription test to verify segments are present in the response. Add a test that segments are `null` for backends that don't support them.

**Acceptance:** Whisper transcriptions include timestamped segments. Parakeet returns null segments. Swift decodes both correctly.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Added worker timestamp segment models:
  - `TranscriptionSegment(start, end, text)`
  - `BackendTranscriptionResult(text, segments)`
  - `FileTranscriptionResponse.segments` (optional, serialized as JSON when present; `null` when absent)
- Updated backend contract to return structured transcription results:
  - `FasterWhisperBackend.transcribe_file()` now maps model segments to `TranscriptionSegment` and returns both full text and segments.
  - `ParakeetBackend.transcribe_file()` now returns `segments=None`.
- Updated `server.py` to pass backend `segments` through in `/transcriptions/file` responses.
- Updated Swift worker payload decoding:
  - `FileTranscriptionResponsePayload` now includes optional `segments` with typed segment payloads.
  - Added tests for both populated `segments` and `segments: null` decoding paths.
- Updated Python worker tests:
  - `test_server.py` verifies segment list payloads and explicit `null` segment payloads.
  - `test_parakeet.py` verifies `segments` is `null` for parakeet responses.

---

### Task B4: Improved Error Reporting in MenuBarView
**Target:** `Sources/WaffleApp/DictationController.swift`, `Sources/WaffleApp/MenuBarView.swift`

Currently errors show raw messages. Make them user-friendly.

1. Map all `WorkerClientError` cases to user-facing messages:
   - Connection refused → "Worker is not running. Restart Waffle."
   - Timeout → "Transcription timed out. Try a shorter recording."
   - 503 (model unavailable) → "Model is still loading. Try again in a moment."
   - 400 (unknown model) → "Selected model is not available in the worker."
2. Add a retry button in the error state that re-attempts the last transcription (keep the scratch file until auto-clear)
3. If the worker crashes during transcription, detect it via health check failure and show "Worker crashed. Restarting…" then auto-restart

**Tests:** None required for UI. Ensure existing tests still pass.

**Acceptance:** Worker errors show friendly messages. Retry button re-sends the last file. Worker crash triggers auto-restart.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Improved worker error messaging in `DictationController.userFacingMessage(for:)`:
  - Connection/host failures map to: `"Worker is not running. Restart Waffle."`
  - Timeout maps to: `"Transcription timed out. Try a shorter recording."`
  - `503` maps to: `"Model is still loading. Try again in a moment."`
  - `400` maps to: `"Selected model is not available in the worker."`
- Added retry-last-transcription flow:
  - Failed transcription keeps the scratch recording and stores retry context (file + model + language).
  - `MenuBarView` now shows a `Retry` button in error state when retry data is available.
  - Retry re-sends the previous recording to the worker.
  - Scratch files are cleaned after success, when a new recording starts, or after error auto-clear timeout.
- Added worker crash detection + auto-restart:
  - On transport/server-failure transcription errors, `DictationController` probes `/health`.
  - If health fails, UI shows `"Worker crashed. Restarting…"` and triggers worker restart.
  - `AppDelegate` now exposes a restart path (`restartWorkerProcess`) and injects it into `DictationController`.
- Verified with full `swift test` (all tests passing).

---

### Task B5: Custom Hotkey Picker
**Target:** `Sources/WaffleApp/SettingsView.swift`, `Sources/WaffleCore/HotkeyService.swift`

Replace the hardcoded ⌥Space with a user-configurable hotkey.

1. Create a `HotkeyRecorderView` in SettingsView:
   - Shows the current hotkey (e.g. "⌥Space")
   - "Change…" button enters recording mode: "Press your shortcut…"
   - Captures the next key combo via `NSEvent.addLocalMonitorForEvents(matching: .keyDown)`
   - Validates: must include at least one modifier (⌘/⌥/⌃/⇧), must not conflict with system shortcuts
   - "Reset to Default" button restores ⌥Space
2. Persist the hotkey in `@AppStorage`:
   - Store as JSON: `{"keyCode": 49, "modifiers": 524288, "displayValue": "⌥Space"}`
3. Update `HotkeyService` to accept a dynamic `GlobalHotkey`:
   - `updateHotkey(_ hotkey: GlobalHotkey)` — stops the current tap, re-registers with new key combo
4. Wire `AppDelegate` to read the stored hotkey on launch and pass it to `HotkeyService`

**Tests:** Not unit-testable (CGEventTap). Ensure `GlobalHotkey` serialization round-trips correctly.

**Acceptance:** User can set a custom hotkey in Settings. The new hotkey works immediately. Restarting the app preserves the choice.

**Dev Notes (30/03/2026):**
- Status: **Done**
- Replaced Keyboard placeholder with a working hotkey recorder in `SettingsView`:
  - New `HotkeyRecorderView` captures key combos via `NSEvent.addLocalMonitorForEvents(matching: .keyDown)`.
  - Validation enforces at least one modifier (`⌘/⌥/⌃/⇧`) and blocks common system conflicts (e.g. Spotlight shortcuts).
  - Added `Reset to Default` action restoring `⌥Space`.
- Added persisted hotkey storage in `@AppStorage("globalHotkey")` using JSON payload:
  - `{"keyCode":..., "modifiers":..., "displayValue":"..."}`
  - Implemented via new `GlobalHotkey.StoragePayload` + JSON encode/decode helpers.
- Updated `HotkeyService` with dynamic update API:
  - `updateHotkey(_:)` now swaps to a new hotkey at runtime.
  - When tap is active, service re-registers with the new key combo immediately.
- Wired `AppDelegate` hotkey lifecycle:
  - Loads stored hotkey on launch (fallback to `⌥Space`).
  - Exposes `updateHotkey(_:)` for settings-driven changes.
  - Persists updates and refreshes hotkey registration.
- Updated menu hint text to show the currently configured hotkey (not hardcoded `⌥Space`).
- Added `Tests/WaffleCoreTests/HotkeyServiceTests.swift` for `GlobalHotkey` JSON round-trip validation.

---

## Priority Order

| Priority | Task | Track | Effort | Gate |
|----------|------|-------|--------|------|
| P0 | A1 — GRDB Transcript Store | A | Medium | None |
| P0 | A2 — Save Dictation to History | A | Small | A1 |
| P0 | A3 — Transcript History View | A | Medium | A1 |
| P1 | A4 — File Import (drag-and-drop) | A | Medium | A1, A3 |
| P1 | A5 — Export (SRT/VTT/JSON/MD) | A | Medium | A1 |
| P1 | B1 — Streaming SHA-256 | B | Small | None |
| P1 | B2 — Language Picker | B | Small | None |
| P2 | B3 — Word-Level Timestamps | B | Medium | None |
| P2 | B4 — Error Reporting Polish | B | Small | None |
| P2 | B5 — Custom Hotkey Picker | B | Medium | None |

Track A tasks are sequential (A1 → A2 → A3 → A4/A5). Track B can be done in parallel with Track A. Within Track B, tasks are independent.

---

## After Phase 3

Once Track A (A1–A5) and Track B (B1–B2 minimum) are done, Phase 3 is complete. The app will:
- Persist all transcription history in a local SQLite database
- Let users search, browse, and manage past transcriptions
- Import audio files for transcription via drag-and-drop
- Export transcripts in 5 formats (Plain Text, Markdown, JSON, SRT, VTT)
- Handle large model downloads without memory spikes
- Support language hints for multilingual transcription

The next step document (Step 6) will cover:
- Phase 4: LM Studio integration (summarise, translate, Q&A on transcripts)
- Phase 5: Ship readiness (pyannote diarization, Sparkle auto-update, code signing, notarization, real CDN model URLs, Ed25519 manifest signing)

---

## Notes for Codex

- **Swift version:** 6.0+ strict concurrency throughout. New types must be `Sendable` where appropriate.
- **Test framework:** Swift Testing (`import Testing`, `@Test`, `#expect`) — NOT XCTest.
- **New dependency: GRDB only.** Add `https://github.com/groue/GRDB.swift` from `7.0.0`. No other new packages.
- **Database location:** `~/Library/Application Support/Waffle/transcripts.sqlite` — use the same `applicationSupportDirectory` pattern as `ModelCatalogService`.
- **FTS5:** GRDB supports FTS5 natively. Use `db.create(virtualTable:using:)` with `FTS5()`.
- **Worker stays at `127.0.0.1:8765`** — don't change the IPC contract.
- **Export formats:** SRT/VTT are single-segment for now (full duration). Word-level segmentation comes from B3, but A5 should work without it.
- **File import accepts:** `.wav`, `.mp3`, `.m4a`, `.flac`, `.ogg`. The worker handles format conversion internally via faster-whisper.
- **No new Python packages** unless required by B3 (faster-whisper already provides segments).
- **History window:** Standard `Window` scene in SwiftUI, not `MenuBarExtra`. Opened via button, not always visible.
- **One file transcription at a time** — same constraint as model downloads. No concurrency queue needed.
